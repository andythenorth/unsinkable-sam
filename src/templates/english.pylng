STR_GRF_NAME                                                    :Unsinkable Sam ${makefile_args.repo_version}

<!--! ship names -->
<tal:build_vehicle_names repeat="ship ships">
STR_NAME_${ship.id}                                          :{STRING} - ${ship.get_name_substr()} ({STRING}) <!--! no space between name and substr -->
</tal:build_vehicle_names>
